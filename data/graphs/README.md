# Universal graph (not in git)

Place `universalGraph_new.json` here. It is gitignored (~1.3 GB).

```bash
cp /path/to/Multi-Omics-AD/artifacts/graphs/ROSMAP/universalGraph_new.json \
   data/graphs/universalGraph_new.json
```
